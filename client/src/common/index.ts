import { PATHES } from "./enum";
import { createNewRoomSchema, loginSchema } from "./schemas";
import { BASE_URL, socket } from "./variables";

export { PATHES, createNewRoomSchema, loginSchema, BASE_URL, socket };
