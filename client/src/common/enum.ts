export enum PATHES {
  LOGIN = "/login",
  MAIN_PAGE = "/",
  ROOM = "/room",
}
export enum RoomStatusEnum {
  WAITING = "Waiting",
  INPROCESS = "InProcess",
  END = "GameEnd",
}

export enum LocalizationLanguagesEnum {
  SPANISH = "spa",
  UKRAINE = "uk",
  ENGLISH = "en",
}
